/** Decorative layers only. The owning button/link keeps its semantics and action. */
export function MetalBorder() {
  return <i className="ip-metal-border" aria-hidden="true"><i className="ip-metal-edge" /></i>;
}

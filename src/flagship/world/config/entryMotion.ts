/** Approved Korea entry timings; keep regional handoffs separate. */
export const entryMotion = {
  approachSeconds: 1.25,
  approachDistance: 2.55,
  pushSeconds: 1.25,
  surfaceDistance: 1.14,
  heroStartScale: 0.68,
  revealStart: 0.58,
  revealEnd: 0.86,
} as const;

import { staticAssetUrl } from "../../../lib/staticAssets";

/** Selected continuous galaxy. Original candidates remain in work/. */
export const worldMaterials = { environment: staticAssetUrl("flagship/stellar-galaxy.webp") };
